---
export interface Props {
  class?: string
}

const { class: className = 'text-primary' } = Astro.props
---

<h1 class={`text-display leading-display max-w-4xl lsa lsa-slide-up no-repeat ${className}`}>
  <slot />
</h1>
